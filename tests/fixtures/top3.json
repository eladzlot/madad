{
  "questionnaire": {
    "id": "top3",
    "title": "שלושת הבעיות המרכזיות",
    "items": [
      { "id": "intro", "type": "instructions", "text": "..." },
      { "id": "p1", "type": "rated_text", "inputType": "multiline", "text": "בעיה ראשונה", "ratingText": "חומרה 1", "min": 1, "max": 12, "required": true },
      { "id": "p1_coping", "type": "text", "inputType": "multiline", "text": "התמודדות 1", "required": false },
      { "id": "p2", "type": "rated_text", "inputType": "multiline", "text": "בעיה שנייה", "ratingText": "חומרה 2", "min": 1, "max": 12, "required": false },
      { "id": "if_p2", "type": "if", "condition": "count(item.p2__text) >= 1",
        "then": [
          { "id": "p2_coping", "type": "text", "inputType": "multiline", "text": "התמודדות 2", "required": false },
          { "id": "p3", "type": "rated_text", "inputType": "multiline", "text": "בעיה שלישית", "ratingText": "חומרה 3", "min": 1, "max": 12, "required": false },
          { "id": "if_p3", "type": "if", "condition": "count(item.p3__text) >= 1",
            "then": [
              { "id": "p3_coping", "type": "text", "inputType": "multiline", "text": "התמודדות 3", "required": false }
            ],
            "else": []
          }
        ],
        "else": []
      }
    ],
    "scoring": {
      "method": "average"
    }
  },
  "cases": [
    {
      "description": "one problem — score is that problem's severity rating",
      "answers": { "p1": 8, "p1__text": "חרדה חברתית" },
      "expected": { "total": 8, "subscales": {}, "category": null }
    },
    {
      "description": "two problems — score is mean of two severity ratings",
      "answers": {
        "p1": 8, "p1__text": "חרדה חברתית",
        "p2": 6, "p2__text": "נדודי שינה"
      },
      "expected": { "total": 7, "subscales": {}, "category": null }
    },
    {
      "description": "three problems — score is mean of three severity ratings",
      "answers": {
        "p1": 9, "p1__text": "חרדה חברתית",
        "p2": 6, "p2__text": "נדודי שינה",
        "p3": 3, "p3__text": "קונפליקט"
      },
      "expected": { "total": 6, "subscales": {}, "category": null }
    },
    {
      "description": "coping text and problem descriptions do not affect score",
      "answers": {
        "p1": 10, "p1__text": "חרדה חברתית", "p1_coping": "הימנעות",
        "p2": 4,  "p2__text": "נדודי שינה",   "p2_coping": "תרופות"
      },
      "expected": { "total": 7, "subscales": {}, "category": null }
    },
    {
      "description": "a described problem with no severity rating is excluded from the mean denominator",
      "answers": {
        "p1": 12, "p1__text": "חרדה חברתית",
        "p2__text": "נדודי שינה"
      },
      "expected": { "total": 12, "subscales": {}, "category": null }
    }
  ]
}
